* GDS_main: Backpropagation-based collection of 8 key detection features for GDS.
* feature_process: Process all sample features and split the training dataset.
* mlp_training: Use the processed features to train an MLP and perform inference to detect member samples.
